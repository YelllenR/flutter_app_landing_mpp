/// Setting the dimensions according to screens

const mobileWidth = 600;
const tabletWidth = 1024;
const desktopWidth = 1200;

const textPaddingMobile = 20;
const textPaddingTablet = 25;
const textPaddingDesktop = 30;

var fontSizeMobile = 40.clamp(20, 30);
var fontSizeTablet = 30.clamp(20, 40);
var fontSizeDesktop = 40.clamp(20, 40);
